spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  ai:
    provider: fake
    api-key: test-key
    base-url: http://localhost
  auth:
    jwt-secret: ${JWT_SECRET}
    token-ttl-seconds: ${JWT_TOKEN_TTL_SECONDS}
